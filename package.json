{
  "name": "agent-tick",
  "version": "1.0.0",
  "description": "Reusable persistent tick daemon for AI agent autonomy",
  "main": "daemon.js",
  "type": "module",
  "scripts": {
    "start": "node daemon.js",
    "dev": "node --watch daemon.js",
    "test": "node --test"
  },
  "dependencies": {
    "chokidar": "^4.0.3",
    "simple-git": "^3.27.0"
  },
  "engines": {
    "node": ">=20"
  }
}
